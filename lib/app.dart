import 'package:flutter/material.dart';

class BazarigoApp extends StatelessWidget {
  const BazarigoApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Center(child: Text('Bazarigo App')),
      ),
    );
  }
}
